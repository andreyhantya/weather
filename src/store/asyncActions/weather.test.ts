import { fetchCityWeather } from './weather';

describe('send mastersRequest', () => {
    it('mastersRequest method shall resolve and return required value', async () => {
        const expected = { status: 200, data: 'Expected data' };
        const dispatch = jest.fn();

        // await thunk(dispatch);
    });
});
